#!/bin/bash
# Reload AeroSpace configuration

echo "🔄 Reloading AeroSpace..."
if aerospace reload-config 2>/dev/null; then
  echo "✅ AeroSpace reloaded successfully"
else
  echo "❌ Failed to reload AeroSpace. Is it running?"
  echo "   Try: aerospace list-workspaces"
  exit 1
fi
