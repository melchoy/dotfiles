# Fresh minimal tmux config
# Basic settings
set -g default-terminal "tmux-256color"
set -g terminal-overrides ",xterm-256color:RGB"

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# Keybindings
bind r source-file ~/.dotfiles/config/tmux/tmux.conf \; display "Config reloaded 🚀"

# Colors (catppuccin macchiato)
set -g mode-style "fg=#24273a,bg=#91d7e3"
set -g message-style "fg=#24273a,bg=#eed49f"
set -g pane-border-style "fg=#6e738d"
set -g pane-active-border-style "fg=#c6a0f6"

# Turn off pane border status (remove top line)
set -g pane-border-status off

# Status bar
set -g status on
set -g status-position bottom
set -g status-style "fg=#cad3f5,bg=#24273a"
set -g status-left-length 50
set -g status-left "#[fg=#24273a,bg=#c6a0f6,bold] hello #[default] > "

# Window status (shows all windows with brackets)
set -g window-status-format "#[fg=#cad3f5]#I:(#W) "
set -g window-status-current-format "#[fg=#f5a97f,bold]#I:(#W) "
set -g window-status-separator ""

set -g status-right ""
